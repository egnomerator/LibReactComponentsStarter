import * as React from "react";
import * as ReactTestRenderer from "react-test-renderer";
import { DevsEditor } from ".";

describe("DevsEditor", () => {
    it("should render (placeholder test)", () => {
        //const devsEditor = ReactTestRenderer.create(<DevsEditor/>);

        //expect(devsEditor).toMatchSnapshot();
    });
});